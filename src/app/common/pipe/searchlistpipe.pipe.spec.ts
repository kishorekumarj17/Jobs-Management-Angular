import { SearchlistpipePipe } from './searchlistpipe.pipe';

describe('SearchlistpipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchlistpipePipe();
    expect(pipe).toBeTruthy();
  });
});
